<h2>Change your password</h2> <!-- Display a heading indicating that the user is in the process of changing their password. -->

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %> <!-- Generate a form for changing the password associated with a specific resource (likely a user). The form is configured to submit a PUT request to the password_path. -->

  <%= render "devise/shared/error_messages", resource: resource, class: "custom-text" %> <!-- Render shared error messages, if any, for the resource (user) with a custom text class. -->

  <%= f.hidden_field :reset_password_token %> <!-- Include a hidden field to store the reset password token, which is crucial for authenticating and authorizing the password change request. -->

  <div class="field">
    <%= f.label :password, class: "field-text", "New password"%><br/> <!-- Display a label for the password input field with a specified CSS class and text. -->

    <% if @minimum_password_length %>
      <em>(<%= @minimum_password_length %> characters minimum, class: "field-text")</em><br /> <!-- If there is a minimum password length requirement, display an informative message about it with a specified CSS class. -->

    <% end %>
    <%= f.password_field :password, autofocus: true, autocomplete: "new-password" %> <!-- Display a password input field, with autofocus enabled and autocomplete set to "new-password." -->

  </div>

  <div class="field">
    <%= f.label :password_confirmation, "Confirm new password" %><br /> <!-- Display a label for the password confirmation input field. -->

    <%= f.password_field :password_confirmation, autocomplete: "new-password" %> <!-- Display a password confirmation input field with autocomplete set to "new-password." -->

  </div>

  <div class="actions">
    <%= f.submit "Change my password" %> <!-- Display a submit button labeled "Change my password" to trigger the form submission. -->

  </div>

<% end %>

<%= render "devise/shared/links" %> <!-- Render shared links, likely related to authentication, such as sign-in or sign-up links. -->