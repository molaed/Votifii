<ul>
    <% if @election.status == 'public' %>
      <%= form_with(model: [@election, Candidate.new], url: increment_votes_election_candidate_path(@election), method: :patch) do |form| %>
        <select name="candidate_id">
          <% @election.candidates.order(:name).each do |candidate| %>
            <%if !candidate.voters.include?(current_user.id)%> <!--Prevent from voting if voted for that candidate-->
              <option value="<%= candidate.id %>"><%= candidate.name %></option>
            <% end %>
          <% end %>
        </select>
        <%= form.submit 'Increment Votes' %>
      <% end %>
    <% end %>
</ul>