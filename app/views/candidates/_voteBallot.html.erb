<!--
<ul>
  <% @election.candidates.each do |candidate| %>
    <li>
      <p><%= candidate.name %></p>
      <p>Votes: <%= candidate.voteCount %></p>
      
      form_with creates the button. 
          url:increment_votes_election_candidate_path(election_id, id) is 
          <function_name>_<nested 'do' from routes.rb>_path(election_id,id)
      
      <%= form_with(model: candidate, url: increment_votes_election_candidate_path(@election, candidate), method: :patch) do |form| %>
        <%= form.submit 'Increment Votes' %>
      <% end %>
    </li>
  <% end %>
</ul>

-->